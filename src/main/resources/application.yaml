server:
  port: 8081

spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      uri: mongodb+srv://_MONGODB_USER_:_MONGODB_PW_@cluster0.jtyq2af.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: yesable
      username: yesable
      password: yesableadmin!
  datasource:
    url: jdbc:mariadb://database-2.c1cqmw8q803p.ap-northeast-2.rds.amazonaws.com:3306/innodb
    username: yesable
    password: yesableadmin!
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MariaDBDialect
          format_sql: true
          use_sql_comments: true

jwt:
  secret: secretKey-authorization-jwt-manage-token
